-- llc_documents is created at boot by server/llc/ensure-tables.ts
-- (CREATE TABLE IF NOT EXISTS, "deploy-proof schema"), including its
-- indexes and unique constraint. Intentionally a no-op for the same
-- reason as 0023 — see that file's comment.
SELECT 1;
