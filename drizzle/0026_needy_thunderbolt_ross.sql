-- retailPaidAt column and paymentLinkUrl column are added at boot by
-- server/llc/ensure-tables.ts (ALTER TABLE IF NOT EXISTS pattern).
-- Intentionally a no-op for the same reason as 0023/0024/0025.
SELECT 1;
